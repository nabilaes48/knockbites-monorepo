# PocketBase Backend
VITE_API_URL=http://localhost:8090
VITE_WS_URL=ws://localhost:8090
ADMIN_EMAIL=admin@camerons.com
ADMIN_PASSWORD=changeme123

# PostgreSQL (if using instead of SQLite)
DB_PASSWORD=dev_password

# MinIO (S3-compatible storage)
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin

# iOS App Local Connection
# Use your Mac's IP address for physical devices
# Find it with: ifconfig | grep "inet " | grep -v 127.0.0.1
IOS_API_URL=http://192.168.1.XXX:8090

# Android App Local Connection
# Android Emulator uses 10.0.2.2 to reach host machine
ANDROID_API_URL=http://10.0.2.2:8090

# Production URLs (when deployed)
# VITE_API_URL=https://api.cameronsconnect.com
# VITE_WS_URL=wss://api.cameronsconnect.com
